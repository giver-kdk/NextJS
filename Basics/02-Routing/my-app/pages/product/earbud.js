// 1st "Next" will search for existing "product_id"(i.e; earbud) named file in the directory
// If file is not found then it uses the dynamically routing page
// To route into this page, goto "/product/earbud"
function EarbudDetail()
{
	return <h1>Separate File Product Detail for ID: earbud</h1>
}
export default EarbudDetail;